<%if resource.avatar%>
	<%= link_to image_tag(resource.avatar.image.thumb("#{size}#").url, class: 'img-thumbnail'), 
		user_path(resource)%>
	<br><br>
<%else%>
	<%= link_to image_tag("/no-avatar.png", class: 'img-thumbnail'), 
		user_path(resource)%>
<%end%>